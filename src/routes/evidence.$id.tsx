import { createFileRoute } from "@tanstack/react-router";
import { PageHeader, OfficialLink, Panel } from "@/components/shared/ui";
import type { Evidence } from "@/types/domain";
import { evidenceService } from "@/services";

export const Route = createFileRoute("/evidence/$id")({ head: ({ params }) => ({ meta: [{ title: `${params.id} Evidence — IP-SAKTI` }, { name: "description", content: "Review evidence text, provenance, authority, and system interpretation." }, { property: "og:title", content: `${params.id} Evidence — IP-SAKTI` }, { property: "og:description", content: "Review evidence provenance and interpretation." }, { property: "og:type", content: "article" }, { name: "twitter:card", content: "summary_large_image" }] }), component: EvidenceDetail, loader: ({ params }) => evidenceService.get(params.id) });
function EvidenceDetail() { const item = Route.useLoaderData() as Evidence | null; if (!item) return <PageHeader eyebrow="Evidence" title="Evidence record not found" description="This evidence reference is not present in the indexed corpus." />; return <><PageHeader eyebrow={item.sourceId} title={item.title} description={`${item.authority} · ${item.jurisdiction}`} actions={<OfficialLink url={item.url} />} /><div className="grid gap-6 xl:grid-cols-[1fr_340px]"><div className="space-y-6"><Panel className="border-l-4 border-l-primary p-6"><p className="section-label">Evidence</p><h2 className="mt-2 text-xl font-semibold">Relevant provision</h2><p className="mt-4 leading-7">{item.provision}</p></Panel><Panel className="border-l-4 border-l-teal bg-accent p-6"><p className="section-label">System interpretation</p><h2 className="mt-2 text-xl font-semibold">Why this matters</h2><p className="mt-4 leading-7 text-muted-foreground">{item.relevance}</p></Panel></div><Panel className="h-fit p-6"><h2 className="text-lg font-semibold">Source record</h2><dl className="mt-5 space-y-4 text-sm">{[["Document", item.title], ["Section", item.section], ["Page", item.page ?? "Not specified"], ["Jurisdiction", item.jurisdiction], ["Version", item.version], ["Effective date", item.effectiveDate], ["Authority level", item.level], ["Verification", item.verified ? "Verified" : "Needs review"]].map(([k,v]) => <div key={k}><dt className="text-xs font-semibold uppercase text-muted-foreground">{k}</dt><dd className="mt-1 font-medium">{v}</dd></div>)}</dl></Panel></div></>; }