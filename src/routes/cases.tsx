import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpDown, Search } from "lucide-react";
import { useMemo, useState } from "react";
import { Input } from "@/components/ui/input";
import { Confidence, EmptyState, PageHeader, Panel, StatusBadge } from "@/components/shared/ui";
import { caseService } from "@/services";

export const Route = createFileRoute("/cases")({ head: () => ({ meta: [{ title: "Cases — IP-SAKTI" }, { name: "description", content: "Search and review Ayurveda classification and IP research cases." }, { property: "og:title", content: "Cases — IP-SAKTI" }, { property: "og:description", content: "Search and review research cases." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: CasesPage, loader: () => caseService.list() });
function CasesPage() { const cases = Route.useLoaderData(); const [query, setQuery] = useState(""); const [filter, setFilter] = useState("all"); const [ascending, setAscending] = useState(false); const filtered = useMemo(() => cases.filter((c) => `${c.product} ${c.id}`.toLowerCase().includes(query.toLowerCase()) && (filter === "all" || c.status === filter)).sort((a,b) => ascending ? a.updated.localeCompare(b.updated) : b.updated.localeCompare(a.updated)), [cases, query, filter, ascending]); return <><PageHeader eyebrow="Research records" title="Cases" description="Search assessment history, monitor reviews, and return to supporting evidence." /><Panel><div className="flex flex-col gap-3 border-b border-border p-5 md:flex-row"><label className="relative flex-1"><Search className="absolute left-3 top-3.5 size-4 text-muted-foreground" /><Input className="pl-9" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search case ID or product..." /></label><select value={filter} onChange={(e) => setFilter(e.target.value)} className="h-11 rounded-lg border border-input bg-card px-3 text-sm"><option value="all">All statuses</option><option value="complete">Complete</option><option value="processing">Processing</option><option value="review">Needs review</option></select><button onClick={() => setAscending((v) => !v)} className="flex min-h-11 items-center justify-center gap-2 rounded-lg border border-input px-4 text-sm font-semibold"><ArrowUpDown className="size-4" />Updated</button></div>{filtered.length ? <div className="divide-y divide-border">{filtered.map((item) => <Link key={item.id} to="/analysis/$id" params={{ id: item.analysisId }} search={{ progress: undefined }} className="grid gap-3 p-5 hover:bg-muted/50 md:grid-cols-[110px_1.4fr_1fr_150px_120px_130px] md:items-center"><strong className="text-sm">{item.id}</strong><div><p className="font-semibold">{item.product}</p><p className="text-xs text-muted-foreground">Reviewer: {item.reviewer}</p></div><span className="text-sm">{item.classification}</span><Confidence value={item.confidence} /><StatusBadge status={item.status} /><span className="text-sm text-muted-foreground">{item.updated}</span></Link>)}</div> : <div className="p-5"><EmptyState title="No matching cases" description="Adjust the search or status filter to see more records." /></div>}<div className="flex items-center justify-between border-t border-border p-4 text-sm text-muted-foreground"><span>Showing {filtered.length} of {cases.length}</span><span>Page 1 of 1</span></div></Panel></>; }