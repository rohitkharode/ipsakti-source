import { createFileRoute, Link } from "@tanstack/react-router";
import { BookOpenText, ChevronRight, Search, SlidersHorizontal } from "lucide-react";
import { useMemo, useState } from "react";
import { Input } from "@/components/ui/input";
import { EmptyState, PageHeader, Panel } from "@/components/shared/ui";
import { evidenceService } from "@/services";

export const Route = createFileRoute("/evidence")({ head: () => ({ meta: [{ title: "Evidence Library — IP-SAKTI" }, { name: "description", content: "Search regulations, formulations, prior art, and authoritative source evidence." }, { property: "og:title", content: "Evidence Library — IP-SAKTI" }, { property: "og:description", content: "Search authoritative Ayurveda, IP, and regulatory evidence." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: EvidencePage, loader: () => evidenceService.list() });
function EvidencePage() { const evidence = Route.useLoaderData(); const [query, setQuery] = useState(""); const [jurisdiction, setJurisdiction] = useState("All"); const results = useMemo(() => evidence.filter((e) => `${e.title} ${e.authority} ${e.provision}`.toLowerCase().includes(query.toLowerCase()) && (jurisdiction === "All" || e.jurisdiction === jurisdiction)), [evidence, query, jurisdiction]); return <><PageHeader eyebrow="Source intelligence" title="Evidence library" description="Search regulations, formulations, patents, traditional-knowledge references, and source records." /><div className="grid gap-6 xl:grid-cols-[260px_1fr]"><Panel className="h-fit p-5"><div className="mb-5 flex items-center gap-2"><SlidersHorizontal className="size-4" /><h2 className="font-semibold">Filters</h2></div>{["Jurisdiction", "Source type", "Authority", "Topic", "Version"].map((name, i) => <label key={name} className="mb-4 block"><span className="mb-1.5 block text-sm font-semibold">{name}</span><select value={i === 0 ? jurisdiction : "All"} onChange={(e) => i === 0 && setJurisdiction(e.target.value)} className="h-11 w-full rounded-lg border border-input bg-card px-3 text-sm"><option>All</option>{i === 0 && <option>India</option>}{i === 1 && <><option>Regulation</option><option>Pharmacopoeia</option></>}{i === 2 && <><option>Primary</option><option>Secondary</option></>}</select></label>)}</Panel><div><label className="relative block"><Search className="absolute left-4 top-3.5 size-4 text-muted-foreground" /><Input className="pl-11" value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search regulations, formulations, patents, sources..." /></label><p className="my-4 text-sm text-muted-foreground">{results.length} evidence records</p>{results.length ? <div className="space-y-3">{results.map((item) => <Link key={item.id} to="/evidence/$id" params={{ id: item.id }} className="research-surface flex gap-4 p-5 hover:border-primary/30 hover:bg-muted/50"><span className="grid size-11 shrink-0 place-items-center rounded-lg bg-info-soft text-info"><BookOpenText className="size-5" /></span><span className="min-w-0 flex-1"><span className="flex flex-wrap items-center gap-2"><strong>{item.title}</strong><span className="rounded-full bg-muted px-2 py-0.5 text-xs font-semibold">{item.level}</span>{item.verified && <span className="rounded-full bg-success-soft px-2 py-0.5 text-xs font-semibold text-success">Verified</span>}</span><span className="mt-1 block text-sm text-muted-foreground">{item.authority} · {item.section} · {item.jurisdiction}</span><span className="mt-3 line-clamp-2 block text-sm">{item.relevance}</span></span><ChevronRight className="mt-3 size-5 shrink-0 text-muted-foreground" /></Link>)}</div> : <EmptyState title="No evidence found" description="Try a broader phrase or remove one of the filters." />}</div></div></>; }