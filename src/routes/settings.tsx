import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { Button } from "@/components/ui/button";
import { PageHeader, Panel } from "@/components/shared/ui";
import { useLanguage } from "@/components/layout/app-shell";
import { LANGUAGES } from "@/lib/i18n";

export const Route = createFileRoute("/settings")({ head: () => ({ meta: [{ title: "Settings — IP-SAKTI" }, { name: "description", content: "Configure IP-SAKTI workspace preferences." }, { property: "og:title", content: "Settings — IP-SAKTI" }, { property: "og:description", content: "Configure workspace preferences." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: SettingsPage });
function SettingsPage() { const { language, setLanguage, t } = useLanguage(); const [saved, setSaved] = useState(false); return <><PageHeader eyebrow="Workspace" title="Settings" description="Manage language and evidence-review preferences for this workspace." /><Panel className="max-w-3xl p-6"><h2 className="text-xl font-semibold">{t("settings.heading")}</h2><div className="mt-6 grid gap-6"><label><span className="mb-2 block text-sm font-semibold">{t("shell.language")}</span><select value={language} onChange={(e) => setLanguage(e.target.value as typeof language)} className="h-11 w-full rounded-lg border border-input bg-card px-3 text-sm">{LANGUAGES.map((item) => <option key={item.code} value={item.code}>{item.label}</option>)}</select><span className="mt-2 block text-sm text-muted-foreground">{t("settings.languageHint")} {t("settings.reasoning")}</span></label><label className="flex min-h-11 items-start gap-3"><input type="checkbox" defaultChecked className="mt-1 size-5 accent-primary" /><span><strong className="block text-sm">{t("settings.uncertainty")}</strong><span className="text-sm text-muted-foreground">{t("settings.uncertaintyHint")}</span></span></label><Button className="w-fit" onClick={() => { setSaved(true); window.setTimeout(() => setSaved(false), 1800); }}>{saved ? t("settings.saved") : t("settings.save")}</Button></div></Panel></>; }